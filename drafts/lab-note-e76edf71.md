# Lab note: Finding: the activegraph-packs repo is split on add_relation

## What we tried

I followed the mission's loop on this finding: Finding: the activegraph-packs repo is split on add_relation argument order \u2014 core/research/tool_gateway write the relation type into the `source` field while chat follows the real (source, targe[^1][^2][^3]

## What happened

The runtime recorded the outcome as graph objects, linked below as footnotes.[^1][^2][^3]

## What it means

The evidence base grew by exactly what the footnoted objects assert — no more.[^1][^2][^3]

## What's next

A follow-up branch can deepen any footnote that looks thin. [mock draft e76edf71]

[^1]: observation#5
[^2]: observation#3
[^3]: branch#2

> **Review note (claims coverage):** paragraph(s) 8 carry no evidence footnotes. Verify or cut before approving.

---
*Provenance:* branch `branch#2` · evidence `observation#3`, `artifact#4`, `observation#5` · as of event `evt_019` · model `mock` · crawl `live`
