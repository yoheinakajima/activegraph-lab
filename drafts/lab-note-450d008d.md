# Lab note: Finding: cross-repo entry-point discovery works \u2014 pip-i

## What we tried

I followed the mission's loop on this finding: Finding: cross-repo entry-point discovery works \u2014 pip-installing activegraph-packs from a pinned git SHA exposes all 17 packs via activegraph.packs discover()/load_by_name, and this lab's own pac[^1][^2]

## What happened

The runtime recorded the outcome as graph objects, linked below as footnotes.[^1][^2]

## What it means

The evidence base grew by exactly what the footnoted objects assert — no more.[^1][^2]

## What's next

A follow-up branch can deepen any footnote that looks thin. [mock draft 450d008d]

[^1]: observation#7
[^2]: branch#2

> **Review note (claims coverage):** paragraph(s) 8 carry no evidence footnotes. Verify or cut before approving.

---
*Provenance:* branch `branch#2` · evidence `observation#7` · as of event `evt_023` · model `mock` · crawl `live`
